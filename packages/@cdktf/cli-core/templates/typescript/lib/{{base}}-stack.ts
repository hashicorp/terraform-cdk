import { Construct } from "constructs";
import { TerraformStack } from "cdktf";

class {{$base}}Stack extends TerraformStack {
  constructor(scope: Construct, id: string) {
    super(scope, id);

    // define resources here
  }
}
