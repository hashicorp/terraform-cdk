{
  "name": "hello-cdktf",
  "version": "0.0.0",
  "main": "index.js",
  "license": "MPL-2.0",
  "scripts": {
    "get": "cdktf get",
    "build": "yarn get && tsc",
    "compile": "tsc",
    "synth": "cdktf synth",
    "plan": "cd ./cdk.out && terraform init && terraform plan",
    "apply": "terraform apply ./cdk.out"
  },
  "devDependencies": {
    "@types/node": "^13.1.1",
    "typescript": "^3.7.4",
    "cdktf-cli": "0.0.0"
  },
  "dependencies": {
    "constructs": "^3.0.0",
    "cdktf": "0.0.0"
  }
}
