{
  "//": "This example is used to verify all code samples in the documentation work correctly.",
  "name": "@examples/python-documentation",
  "version": "0.14.1-dev.1674818835",
  "license": "MPL-2.0",
  "scripts": {
    "reinstall": "rm Pipfile.lock && PIPENV_IGNORE_VIRTUALENVS=1 pipenv install ./../../../dist/python/*.whl",
    "build": "cdktf get --parallelism 1",
    "synth": "cdktf synth"
  }
}
